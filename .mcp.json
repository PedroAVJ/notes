{
  "mcpServers": {
    "apple-notes": {
      "type": "stdio",
      "command": "/bin/sh",
      "args": [
        "-c",
        "root=$CLAUDE_PLUGIN_ROOT; if [ -z \"$root\" ]; then root=.; fi; exec \"$root/scripts/launch-apple-notes-mcp\""
      ],
      "cwd": "."
    }
  }
}
